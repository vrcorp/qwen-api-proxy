# ============================================
# Qwen API Proxy - Environment Configuration
# ============================================

# ─── Server Configuration ───────────────────
PORT=3264
HOST=0.0.0.0

# ─── Logging ──────────────────────────────
LOG_LEVEL=info

# ─── Authentication Tokens ──────────────────
# You can configure tokens in three ways:
# 1. Via this .env file (convenient for deployment)
# 2. Via session/tokens.json (using 'npm run addToken')
# 3. Both (tokens from both sources are combined)

# QWEN_TOKEN: Single token configuration
# Copy your token from: https://chat.qwen.ai → F12 → Application → Local Storage → token
# QWEN_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# QWEN_TOKENS: Multiple tokens (comma-separated)
# Use this for load balancing across multiple accounts
# QWEN_TOKENS=token1,token2,token3

# Example with multiple tokens:
# QWEN_TOKENS=eyJhbGciOiJIUzI1Ni...,eyJhbGciOiJIUzI1Ni...,eyJhbGciOiJIUzI1Ni...

# ─── Notes ───────────────────────────────────
# • Tokens from .env are loaded automatically on server start
# • Each token is given an auto-generated ID (env_token_1, env_token_2, etc.)
# • Environment tokens take priority over session/tokens.json
# • Keep your tokens secret! Never commit .env to git
# • Get your token: https://chat.qwen.ai → Login → F12 → Application → Local Storage → token

# ─── API Keys for Proxy Authentication (Optional) ───────────────
# If set, clients must provide one of these keys via Authorization header
# Leave empty to disable proxy authentication
# Format: Comma-separated list of keys
# API_KEYS=secret-key-1,secret-key-2,secret-key-3

# Example:
# API_KEYS=my-production-key,my-dev-key